{
  "rules": [
    {
      "default": {
        "targetService": "echo",
        "pathPrefix": "/echo",
        "dropPrefix": true
      },
      "endpoints": [
        {
          "method": "GET",
          "pathPattern": ".*"
        }
      ]
    }
  ]
}